version: '3.8'

services:
  checkon-server:
    build: .
    image: gospel_app:latest
    container_name: checkon_server
    restart: always
    init: true
    stop_grace_period: 10s
    ports:
      - "3033:3033"
    volumes:
      - ./uploads:/app/server/uploads
    environment:
      - TZ=Asia/Seoul
      - PORT=3033
      - NODE_ENV=production
      - DATABASE_URL=mysql://changwon:Changwon0691!@jbchcw.com:3307/jbchcwDB
      - DB_HOST=jbchcw.com
      - DB_PORT=3307
      - DB_USER=changwon
      - DB_PASSWORD=Changwon0691!
      - DB_NAME=jbchcwDB

