version: '3.8'

services:
  checkon-server:
    build: .
    image: gospel_app:latest
    container_name: checkon_server
    restart: always
    ports:
      - "3033:3033"
    environment:
      - PORT=3033
      - NODE_ENV=production
      - DB_HOST=jbchcw.com
      - DB_PORT=3307
      - DB_USER=changwon
      - DB_PASSWORD=Changwon0691!
      - DB_NAME=jbchcwDB

